export enum ActionModalState {
    success = "success",
    failed = "failed",
    warning = "warning",
    danger = "danger",
    pending = "pending",
    delete = "delete",
    reject = "reject",
    locked = "locked"
}
  
export enum ActionModalType {
    info = "info",
    confirm = "confirm"
}